from typing import Iterator, Tuple


def sample_pgn(path: str) -> Iterator[str]:
    _ = path
    if False:
        yield ""


def build_dataset(pgn_path: str) -> Iterator[Tuple[str, int]]:
    _ = pgn_path
    if False:
        yield ("", 0)
